# VAPTForge Enterprise — Python Dependencies
# Install: pip install -r requirements.txt

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1

# Data validation
pydantic==2.7.3
pydantic-settings==2.3.3

# Database (async SQLite)
sqlalchemy==2.0.31
aiosqlite==0.20.0

# Authentication
# IMPORTANT: bcrypt must be 4.x — version 5.x breaks passlib
bcrypt==4.0.1
passlib==1.7.4
python-jose[cryptography]==3.3.0

# HTTP client (scanner engine)
httpx==0.27.0
beautifulsoup4==4.12.3
lxml==5.2.2

# Form data / file uploads
python-multipart==0.0.9

# Environment variables
python-dotenv==1.0.1

# PDF report generation
weasyprint==62.3
reportlab==4.2.2

# v2.0 — Deep scanning dependencies
playwright==1.44.0        # Headless browser for DOM XSS detection
# After pip install, run: playwright install chromium

# v2.0 Scanner upgrades
playwright==1.44.0          # Headless browser — DOM XSS, SPA scanning
# Install browsers after: playwright install chromium

# ML severity prediction (v2.1)
scikit-learn==1.5.0
numpy==1.26.4
